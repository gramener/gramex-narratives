validate:
  image: gramener/builderrors
  script: builderrors

deploy:
  stage: deploy
  script: deploy
  only: [master, dev]
  variables:
    SERVER: gramener.com
    URL: dynarrate
    VERSION: static
